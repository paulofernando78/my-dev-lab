import { pushPop } from "./push-pop.js";
import { shiftUnshift } from "./shift-unshift.js";
import { splice } from "./splice.js";
import { sortReverse } from "./sort-reverse.js";

export const section42 = {
  sectionId: "mutating-methods",
  sectionLabel: "4.2 Mutating Methods",
  sectionAriaLabel: "4.2 Mutating Methods",
  subSections: [
    pushPop,
    shiftUnshift,
    splice,
    sortReverse,
  ],
};
