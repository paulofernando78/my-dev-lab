import { globalScope } from "./global-scope.js";
import { blockScope } from "./block-scope.js";
import { varLetConstScope } from "./var-let-const-scope.js";

export const section15 = {
  sectionId: "scope",
  sectionLabel: "1.5 Scope",
  subSections: [
    globalScope,
    blockScope,
    varLetConstScope,
  ],
};
