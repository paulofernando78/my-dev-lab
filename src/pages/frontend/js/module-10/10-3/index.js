import { postRequests } from "./post-requests.js";
import { requestHeaders } from "./request-headers.js";
import { jsonStringify } from "./json-stringify.js";

export const section103 = {
  sectionId: "sending-data",
  sectionLabel: "10.3 Sending Data",
  subSections: [postRequests, requestHeaders, jsonStringify],
};
